<!DOCTYPE html>
<html>
    <head>
        <title><%= actor.first_name %> <%= actor.last_name %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>

    <body>
        
        
        <h1> Actor : <%= actor.first_name %> <%= actor.last_name %></h1>

        <ul>
            <% for( let i = 0; i < actor.movies.length; i++ ) { %>
            
                <li> <%= actor.movies[i].title %>  </li>
            <% } %>
            
        </ul>
        
       
    </body>
</html>
